"""Reproducibility utilities for paper-only analyses.

These modules only *read* the existing ``outputs/`` tree. New analysis artifacts
are written to ``paper_artifacts/`` (or a user-specified directory).
"""
